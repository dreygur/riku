# GitHub Actions E2E workflow — commented out to avoid Actions costs.
# To enable: remove the comment markers below and push to your repo.
#
# name: E2E Tests
# on: [push, pull_request]
# jobs:
#   e2e:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Run Docker E2E tests
#         run: ./scripts/run-docker-tests.sh
